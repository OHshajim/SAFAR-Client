
const SpotsCard = ({ spot }) => {
    const { name, email, spotName, location, country, seasonality, travel_time, visitorsPerYear, average_cost, image, description } = spot;
    return (
        <div>

        </div>
    );
};

export default SpotsCard;